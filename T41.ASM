.386
DATA SEGMENT USE16
W		DW	200
X		DW	100
Y		DW	10
ANS		DW	0
DATA ENDS

STACK SEGMENT USE16 STACK
		DW 	100 DUP(0)
STACK ENDS

CODE SEGMENT USE16
		ASSUME CS: CODE, DS: DATA, SS: STACK
START:	MOV AX, DATA
		MOV DS, AX
		MOV AX, STACK
		MOV SS, AX
		MOV AX, W
		SUB AX, X
		XOR DX, DX
		MOV BX, 10
		IDIV BX
		IMUL Y
		MOV ANS, AX
		MOV AL, 4CH
		INT 21H
CODE ENDS
	END START