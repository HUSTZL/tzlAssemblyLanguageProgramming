.386
DATA SEGMENT USE16
BUF     DW 2,5,-3,6,38,0,-20,-9,-66,20
N       EQU ($-BUF)/2
DATA ENDS

STACK SEGMENT USE16 STACK
        DW 100 DUP(0)
STACK ENDS

CODE SEGMENT USE16
        ASSUME CS:CODE, DS:DATA, SS:STACK
START:  MOV AX, DATA
        MOV DS, AX
        MOV CX, N
        MOV AX, 7999H
        MOV SI, OFFSET BUF
LOOPA:  CMP AX, WORD PTR [SI]
        JLE NOTLOW
        MOV AX, WORD PTR [SI]
NOTLOW: ADD SI, 2
        LOOP LOOPA
        MOV CX, AX
        MOV AH, 4CH
        INT 21H
CODE ENDS
        END START
        
