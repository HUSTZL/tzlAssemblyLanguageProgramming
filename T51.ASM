.386
DATA SEGMENT USE16
BUF		DB 200 DUP(0FFH)
DATA ENDS

STACK SEGMENT USE16 STACK
		DW 200 DUP(0)
STACK ENDS

CODE SEGMENT USE16
	ASSUME CS:CODE, DS:DATA, ES:DATA, SS:STACK
START:	MOV AX, DATA
		MOV DS, AX
		MOV ES, AX
		MOV CX, 100
		MOV AL, 0
		LEA DI, BUF
		CLD
		REP STOSB
		MOV AH, 4CH
		INT 21H
CODE ENDS
	END START